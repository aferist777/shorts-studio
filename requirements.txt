PySide6>=6.6
keyring>=24          # API keys live in the OS credential store, never in repo files
anthropic>=0.116     # official SDK — the only supported way to call Claude
httpx>=0.27          # OpenAI-compatible providers (OpenRouter, DeepSeek, Ollama)
edge-tts>=7.0        # free TTS with word-level timings (phase 2)
